<div>
  <% @students.each do |student| %>
    <div><h1><%= student.first_name%></h1> <h1><%= student.last_name %></h1>
    <a href="students/<%= student.id %>"><%= student.first_name %></a>
    </div>
  <% end %>
</div>
